<!--<p>-->
  <%#= button_to "Delete", tweet_comment_path(tweet, comment), method: :delete, data: { confirm: "Are you sure you want to delete this comment?" }, class: "btn btn-danger btn-sm mt-2" %>
<!--</p>-->
<%= form_with(model: [tweet, comment], method: :delete, local: true) do %>
  <%= submit_tag "Delete", class: "btn btn-danger btn-sm" %>
<% end %>
